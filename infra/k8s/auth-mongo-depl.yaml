apiVersion: apps/v1
kind: Deployment
metadata:
  name: tsm-auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tsm-auth-mongo
  template:
    metadata:
      labels:
        app: tsm-auth-mongo
    spec:
      containers:
        - name: tsm-auth-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: tsm-auth-mongo-srv
spec: 
  selector:
    app: tsm-auth-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017